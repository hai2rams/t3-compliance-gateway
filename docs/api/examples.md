# T3 Compliance Gateway — examples

Add real examples here as you index the API. Cursor learns patterns faster from concrete payloads than from endpoint lists alone.

## Template

### Example: _short name_

```bash
curl -X POST "https://REPLACE_WITH_GATEWAY_HOST/v1/REPLACE_PATH" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer REPLACE_TOKEN" \
  -d '{
    "field": "value"
  }'
```

**Response (200):**

```json
{
  "status": "ok"
}
```

---

_Add more examples below._
